export interface MAT_DIALOG_HEADER {
    hideIcons : boolean,
    header : string,
    searchBox?:boolean,
    id?:string,
    maxWidth?: string,
    maxHeight?:string,
    defaultWidth?:string,
    defaultHeight?:string
}